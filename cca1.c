#include <stdio.h>
#include <string.h>

main{
	x = 1;
	String cmd;
	while (x = 1){
		
		switch(cmd){
			case (pwd || pw):
			
		}
		
		
		
		
		
	}


}

if[cmd = ifc]{
	ifconfig | grep "eth0" -A 8
else
	ifconfig | grep "INPUT:4" -A 8
}